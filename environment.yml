name: dgm4plus
channels: [pytorch, nvidia, conda-forge]
dependencies:
  - python=3.10
  - pytorch>=2.2
  - torchvision>=0.17
  - cudatoolkit=12.1  # remove if CPU-only
  - pip
  - pip:
      - -r requirements.txt
